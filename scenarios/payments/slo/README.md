# Payments — SLO spec

Capture service objectives and error-budget policies for payments. Validate updates with `python scripts/validate-slos.py scenarios/payments/slo/slo-spec.yml` and log changes in the playbooks.

---

Define los objetivos de servicio y políticas de error budget para pagos. Valida actualizaciones con `python scripts/validate-slos.py scenarios/payments/slo/slo-spec.yml` y documenta ajustes relevantes en los playbooks.
