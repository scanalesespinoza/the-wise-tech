name: auto-label-feedback
on:
  issues:
    types: [opened, edited]
jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Auto label by title/labels (no-op if already set)
        run: |
          echo "Relying on template default labels (feedback/user-experience, ops/postmortem, enhancement/proposal)."
