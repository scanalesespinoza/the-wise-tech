# Service Observability

This example emits structured logs and simulates trace/span propagation. It shows
how the component behavior contract captures telemetry coverage and alert
routing.

```bash
python service.py
```

Inspect the console output to see JSON logs and span identifiers referenced by
`behavior-contract.yaml`.
